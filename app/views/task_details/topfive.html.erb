<br/>
<h1>ToDo</h1>

<div class="row">
  <table class="table">
    <thead>
      <tr>
        <th scope="col">Email</th>
        <th scope="col">Titulo Tarea</th>
        <th scope="col">Completada</th>
      </tr>
    </thead>
    <tbody>

              <% @task_detail.each do |x| %>
                <tr>
                    <th scope="row"><%= x.user.email %></th>
                    <td><%= x.task.name %></td>
                    <td>SI</td>
                </tr>
              <%end%>
      </tbody>
  </table>
  </div>
